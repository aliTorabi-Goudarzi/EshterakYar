# گزارش توسعه: فاز ۱ - ویژگی ۱ (پشتیبانی تقویم شمسی)

**تاریخ:** 2025-12-25
**توسعه‌دهنده:** Antigravity Agent

## 📋 خلاصه تغییرات
در این مرحله، زیرساخت اصلی برای پشتیبانی عمیق از تقویم شمسی (جلالی) در لایه Domain پیاده‌سازی شد و همچنین در لایه Presentation برای نمایش تاریخ‌های شمسی ادغام گردید.

## 🛠️ جزئیات پیاده‌سازی

### ۱. مدل داده‌ای `JalaliDate` (Domain Layer)
- مسیر: `ir.dekot.eshterakyar.core.domain.model.JalaliDate`
- یک `data class` غیرقابل تغییر (Immutable) برای نگهداری تاریخ شمسی.
- **اعتبارسنجی (Validation):** در زمان ساخت آبجکت، ورودی‌ها (سال، ماه، روز) چک می‌شوند و در صورت نامعتبر بودن خطا پرتاب می‌شود.
- **محاسبات کبیسه:** الگوریتم ۳۳ ساله برای تشخیص سال‌های کبیسه پیاده‌سازی شد.
- **طول ماه:** منطق محاسبه طول ماه (۶ ماه اول ۳۱ روز، ۵ ماه دوم ۳۰ روز، ماه آخر ۲۹ یا ۳۰ روز).
- **فرمت‌دهی:** متدهای `toString()` و `format()` برای نمایش تاریخ (مانند `1403/01/01`) اضافه شدند.
- **نام ماه:** متد `monthName()` برای دریافت نام فارسی ماه.

### ۲. مبدل تاریخ `DateConverter` (Domain Layer)
- مسیر: `ir.dekot.eshterakyar.core.domain.utils.DateConverter`
- تبدیل دوطرفه بین `java.time.LocalDate` (میلادی) و `JalaliDate`.
- از الگوریتم استاندارد تبدیل روز-شماری (JDN/Day Count) برای دقت بالا استفاده شد.

### ۳. بروزرسانی رابط کاربری (UI Layer)
کامپوننت‌های زیر برای نمایش تاریخ شمسی بجای میلادی بروزرسانی شدند:
- **`SubscriptionCard`:** نمایش تاریخ تمدید بعدی در لیست اشتراک‌ها.
- **`ScheduleStep`:** نمایش تاریخ انتخاب شده در فرم افزودن اشتراک (مرحله زمان‌بندی).
- **`ReviewStep`:** نمایش تاریخ نهایی در مرحله بررسی و ذخیره.

## ✅ تست و اعتبارسنجی (TDD)
طبق قانون اساسی (Constitution)، ابتدا تست‌ها نوشته شدند و سپس کد پیاده‌سازی شد.

### تست‌های نوشته شده:
1. **`JalaliDateTest`:**
   - ساخت تاریخ معتبر.
   - پرتاب خطا برای تاریخ نامعتبر.
   - بررسی طول ماه‌ها در سال کبیسه و عادی.
   - تشخیص صحیح سال کبیسه (مثلاً ۱۴۰۳).

2. **`DateConverterTest`:**
   - تبدیل دقیق نوروز ۱۴۰۳ (2024-03-20).
   - تبدیل رفت و برگشت (Round-trip) برای اطمینان از صحت الگوریتم.
   - بررسی هندل کردن سال‌های کبیسه در تبدیل.

## 📝 وضعیت فعلی
ویژگی "پشتیبانی تقویم شمسی" اکنون در مرحله بهره‌برداری است. کاربران تاریخ‌ها را در تمام بخش‌های اصلی به صورت شمسی مشاهده می‌کنند.

---
**فایل‌های ایجاد/ویرایش شده:**
- `core/domain/model/JalaliDate.kt`
- `core/domain/utils/DateConverter.kt`
- `test/.../JalaliDateTest.kt`
- `test/.../DateConverterTest.kt`
- `feature_home/.../SubscriptionCard.kt`
- `feature_home/.../SubscriptionDetail.kt`
- `feature_home/.../EditSubscription.kt`
- `feature_addSubscription/.../ScheduleStep.kt`
- `feature_addSubscription/.../ReviewStep.kt`
- `feature_addSubscription/.../SubscriptionItem.kt`
- `feature_addSubscription/.../SubscriptionDetailBottomSheet.kt`
- `screens/ProfileDetailScreen.kt`
- `screens/personalInfo/screen/PersonalInformationScreen.kt`
